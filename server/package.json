{
  "scripts": {
    "db:up": "dbmate up",
    "db:down": "dbmate down",
    "db:new": "dbmate new",
    "db:status": "dbmate status",
    "db:gen:sql": "sh -c 'set -a && [ -f .env ] && . ./.env; set +a; exec gleam run -m squirrel'",
    "db:gen:sql:check": "sh -c 'set -a && [ -f .env ] && . ./.env; set +a; exec gleam run -m squirrel check'"
  },
  "devDependencies": {
    "dbmate": "^2.15.0"
  }
}
